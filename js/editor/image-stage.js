// AI TODO: draw background image into #stage, maintain scale, convert clicks to normalized coords (0..1).
// Expose addAnchor({id,label,type,x,y,accepts,isDistractor}) and render anchor dots in editor.
